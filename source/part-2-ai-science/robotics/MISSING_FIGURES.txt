RL_Rover_Handout.tex does NOT compile as-is: three images it references are
missing from the Prism export and were not found anywhere on disk. They live
only in the Prism project.

Needed (drop them into Figures/ next to this file):
  Figures/cutebot.png
  Figures/microbit_v2.png
  Figures/rover_exoplanet_hero.png

Note: the Prism export also contained generic images (TAI_STEM/image*.png) that
may be these three under Prism's auto-generated names, but the mapping is not
certain, so they were not added here. Confirm with Andrea.

(It also uses the standard `booktabs` package, which auto-installs on a normal
TeX setup.)
